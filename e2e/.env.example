# Copy to e2e/.env (gitignored) for scratch real-environment verification. Environment variables override this file.
# OpsNap instance under verification
OPSNAP_BASE_URL=http://127.0.0.1:8210
# opsnap-test services on docker.local (see deploy/test/docker-compose.yaml and docs/verification.md#test-environment)
TEST_ENV_HOST=192.168.8.141
# Shared test password, generated by `scripts/test-env.sh up` on first run
OPSNAP_TEST_PASSWORD=
