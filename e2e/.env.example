# 复制为 e2e/.env（不提交），用于 scratch 真实环境验证。环境变量优先于本文件。
# 被验证的 OpsNap 实例地址
OPSNAP_BASE_URL=http://127.0.0.1:8210
# docker.local 上 opsnap-test 测试服务（见 deploy/test/compose.yaml 与 docs/verification.md）
TEST_ENV_HOST=192.168.8.141
# 测试服务密码：由 scripts/test-env.sh up 首次运行时生成
OPSNAP_TEST_PASSWORD=
